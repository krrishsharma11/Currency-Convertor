{/*import axios from 'axios';

export default async function fetchExchangeRates() {
  const API_KEY = 'cur_live_L69SUHrU9X1FH6YYdxIcOAhZXazamfuM55nM8jTv'; // Your API key
  const URL = `https://openexchangerates.org/api/latest.json?app_id=${API_KEY}`;

  try {
    const response = await axios.get(URL);
    return response.data.rates; // Returns the exchange rates
  } catch (error) {
    throw new Error('Failed to fetch exchange rates');
  }
} */}